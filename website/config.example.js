// Copy this file to config.js and fill in your values.
// config.js is gitignored — never commit real keys.
// Restrict the YouTube API key by HTTP referrer in Google Cloud Console.
window.SITE_CONFIG = {
  YOUTUBE_API_KEY: '',
  YOUTUBE_CHANNEL_ID: '',
};
